<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tkmce.hestia20">

    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">

        <activity android:name=".login_page.LoginPage" />
        <activity android:name=".login_page.SplashActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:name=".home_main.HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity android:name=".home_main.AboutActivity" />
        <activity android:name=".home_main.SponsorsActivity" />
        <activity android:name=".user_dash.UserHome" />
        <activity android:name=".EventDetailed.ActivityRegister"></activity>
        <activity android:name=".EventDetailed.EventDetailed" />

    </application>

</manifest>